function blender()
{
    var mango = "2kg";
    console.log("mangoes quantity: "+mango);
}

// blender();

///////////sum function
function sum(a,b)
{
    var sums = a + b;
    console.log("sum of " + a+ " and " + b + " is " +sums);
    return sums;  //return ... → Gives a value back from a function so it can be used elsewhere in your code. It doesn’t automatically print.
}

sum (100,50000);
